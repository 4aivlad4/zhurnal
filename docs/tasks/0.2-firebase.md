# 0.2 База и проверка связи

Этап 0 · Размер M · Статус ⬜ · 👤

## Зачем
Убедиться, что бесплатная база Firebase работает из России на телефонах семьи
и на моноблоке, — до того как строить на ней журнал.

## Прочитать
- `docs/architecture.md` — «Хранилище», «Вход и доступ», «Запасной план хранилища»
- `docs/data-model.md` — «Правила доступа»
- `docs/decisions.md` — Р-3, Р-8, В-4

## Владелец 👤 (≈20 минут, удобнее с компьютера)
1. console.firebase.google.com → войти Google-аккаунтом → **Создать проект**
   (имя, например, `zhurnal-centra`); Google Analytics — выключить.
   Консоль не открывается — написать Claude: решим (VPN один раз для настройки
   или запасной вариант).
2. **Build → Firestore Database → Create database**: редакция Standard (если спросит),
   расположение **europe-west3 (Frankfurt)** — потом не меняется, режим **production**.
3. **Build → Authentication → Get started → Email/Password** → включить.
   **Users → Add user** — по одному на человека: `mama@example.com`, `papa@example.com`,
   `doch@example.com` и свой. Почты выдуманные, письма не приходят. Пароли — от 6 символов,
   записать у себя, в чат не присылать.
   **Settings → User actions** → снять «Enable create (sign-up)» (если пункта нет — не страшно:
   защищают правила доступа).
4. ⚙ **Project settings → Ваши приложения →** значок `</>` → зарегистрировать веб-приложение →
   скопировать блок `firebaseConfig` и прислать в чат (это не секрет).
   Из **Authentication → Users** прислать колонку **User UID** (тоже не секрет).
5. Когда Claude подготовит правила: **Firestore → Rules** → заменить текст содержимым
   `firebase/firestore.rules` → **Publish**.
6. Желательно: ограничить ключ адресом сайта (Google Cloud Console → Credentials →
   Browser key → HTTP referrers). Claude подскажет точный адрес.

## Сделать (Claude)
1. Firebase SDK (app, auth, firestore; зафиксированная версия) → `app/vendor/firebase/`.
   Абсолютные ссылки импорта внутри файлов заменить на относительные;
   версию записать в `app/vendor/README.md`.
2. `app/js/config.js` — присланный `firebaseConfig`.
3. `app/js/db-firebase.js` — минимум: инициализация с офлайн-кэшем, вход,
   запись и чтение служебного документа `meta/ping`.
4. Временный экран «Проверка связи»: кнопки «Мама / Папа / Дочь / Другой» + пароль →
   «Проверить» → «Связь есть: запись 0,4 с, чтение 0,2 с» или понятная ошибка.
5. `firebase/firestore.rules` с присланными UID.
6. Если В-1 = б): публикация на Firebase Hosting + пошаговая инструкция владельцу
   по ключу для GitHub.

## Не делать
- Структуру журнала и экраны — это 1.1 и дальше.

## Готово, когда
- [ ] «Связь есть» на телефоне мамы — и через мобильный интернет, и через Wi-Fi центра
- [ ] «Связь есть» на моноблоке
- [ ] без входа база данные не отдаёт (правила работают)
- [ ] замеры записаны в «Итог»
- [ ] не работает из РФ — остановиться и вынести владельцу вопрос о запасном варианте

## Итог
_Заполняется в конце._
