// Публичные настройки Firebase. Это не секрет: так устроен Firebase, данные защищают правила доступа
// (firebase/firestore.rules). Значения берутся из консоли Firebase (карточка docs/tasks/0.2-firebase.md).
// Пока проект не создан, здесь null — экран проверки скажет «База ещё не подключена».
export const firebaseConfig = null;
